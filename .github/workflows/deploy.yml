name: 🚀 QwikBrew — CI/CD Pipeline (Fixed)

on:
  push:
    branches: [main, staging, develop]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  AWS_REGION: ap-south-1
  ECR_REGISTRY: ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.ap-south-1.amazonaws.com
  JAVA_VERSION: "17"
  MAVEN_OPTS: "-Xmx1g -XX:+UseG1GC"

jobs:

# ═══════════════════════════════════════════════════════════════
# 1. DETECT SERVICES
# ═══════════════════════════════════════════════════════════════
  detect:
    runs-on: ubuntu-latest
    outputs:
      services: ${{ steps.set.outputs.services }}

    steps:
      - uses: actions/checkout@v4

      - name: Set services
        id: set
        run: |
          SERVICES=("api-gateway" "user-service" "menu-service" "order-service" "payment-service" "notification-service")
          JSON=$(printf '%s\n' "${SERVICES[@]}" | jq -R . | jq -sc .)
          echo "services=$JSON" >> $GITHUB_OUTPUT

# ═══════════════════════════════════════════════════════════════
# 2. BUILD + PUSH DOCKER
# ═══════════════════════════════════════════════════════════════
  build:
    runs-on: ubuntu-latest
    needs: detect
    strategy:
      matrix:
        service: ${{ fromJson(needs.detect.outputs.services) }}

    steps:
      - uses: actions/checkout@v4

      - name: ☕ Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: temurin
          cache: maven

      - name: 🔨 Build JAR
        run: |
          mvn install -N -DskipTests -q
          mvn package -pl ${{ matrix.service }} -DskipTests

      - name: 🏷️ Tag image
        id: tag
        run: |
          TAG=${{ github.ref_name }}-$(echo ${{ github.sha }} | cut -c1-7)
          IMAGE=${{ env.ECR_REGISTRY }}/qwikbrew/${{ matrix.service }}
          echo "tag=$TAG" >> $GITHUB_OUTPUT
          echo "image=$IMAGE:$TAG" >> $GITHUB_OUTPUT

      - name: 🔐 AWS Login
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: 🔑 ECR Login
        uses: aws-actions/amazon-ecr-login@v2

      - name: 🐳 Build & Push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile
          push: true
          tags: |
            ${{ steps.tag.outputs.image }}
          build-args: |
            SERVICE=${{ matrix.service }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

# ═══════════════════════════════════════════════════════════════
# 3. TRIVY SCAN
# ═══════════════════════════════════════════════════════════════
  scan:
    runs-on: ubuntu-latest
    needs: build
    strategy:
      matrix:
        service: ${{ fromJson(needs.detect.outputs.services) }}

    steps:
      - name: 🔐 AWS Login
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - uses: aws-actions/amazon-ecr-login@v2

      - name: 🔍 Scan image
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ env.ECR_REGISTRY }}/qwikbrew/${{ matrix.service }}:${{ github.ref_name }}-$(echo ${{ github.sha }} | cut -c1-7)
          severity: HIGH,CRITICAL
          exit-code: 0

# ═══════════════════════════════════════════════════════════════
# 4. DEPLOY TO EKS
# ═══════════════════════════════════════════════════════════════
  deploy:
    runs-on: ubuntu-latest
    needs: scan

    steps:
      - uses: actions/checkout@v4

      - name: 🔐 AWS Login
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: ☸️ Connect to EKS
        run: |
          aws eks update-kubeconfig --name qwikbrew --region $AWS_REGION

      - name: 🚀 Deploy
        run: |
          kubectl apply -f k8s/

      - name: ⏳ Wait for rollout
        run: |
          for svc in api-gateway user-service menu-service order-service payment-service notification-service
          do
            kubectl rollout status deployment/$svc -n qwikbrew --timeout=300s || true
          done

# ═══════════════════════════════════════════════════════════════
# 5. ROLLBACK (AUTO)
# ═══════════════════════════════════════════════════════════════
  rollback:
    runs-on: ubuntu-latest
    needs: deploy
    if: failure()

    steps:
      - name: 🔐 AWS Login
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: ☸️ Connect
        run: aws eks update-kubeconfig --name qwikbrew --region $AWS_REGION

      - name: ⏪ Rollback
        run: |
          for svc in api-gateway user-service menu-service order-service payment-service notification-service
          do
            kubectl rollout undo deployment/$svc -n qwikbrew
          done
